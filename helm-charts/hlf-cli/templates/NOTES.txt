Run the following commands to...
1. Get the name of the pod running the Fabric Peer:
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app={{ include "hlf-cli.name" . }},release={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")